# activation caches and large arrays (regenerate with --stage extract)
artifacts/*.npz
artifacts/*.npy
artifacts/*.log
# dataset must NOT be committed (GAIA content is gated); it lives in ../data
../data/
__pycache__/
.pytest_cache/
backup_*/
*.docx

# raw transcript text from the gated dataset: regenerate locally, never publish
artifacts/inversion_transcripts.txt
# exports
*.zip
export_final/
