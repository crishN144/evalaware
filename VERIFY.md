# VERIFY

The verification record is [VERIFY_CRISH.md](VERIFY_CRISH.md): what was checked, the
command, what came out, the verdict, for each headline claim. `./verify.sh` re-runs all
of it and writes a timestamped log to `artifacts/`. This file is a pointer so the record
is not duplicated and allowed to drift.
